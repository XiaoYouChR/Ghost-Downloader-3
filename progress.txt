# 进度日志

压缩说明: 本文件已在 2026-04-28 压缩，保留已完成任务标题、范围、验证结论和对齐摘要；历史逐命令明细与完整差异由 Git 提交记录追溯。
任务完成状态以 task.json 为唯一来源；后续轮次继续按 AGENTS.md 追加新的完整中文证据条目。

## 2026-04-13 - 任务: Bootstrap Codex automation workflow
### 已完成内容:
- 已完成任务 #1: Create the repository-level automation contract, task source, progress log, runner script, and minimal validation so Codex can execute one task per round with auditable state transitions.
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 建立 manifest 与 Pack loader 基线测试
### 已完成内容:
- 已完成任务 #2: 先为现有 `features/*/manifest.toml` 和当前 Pack 发现/排序流程建立基线测试，保证后续重写 loader 时有明确护栏。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 建立 Task 与 TaskStage 持久化基线测试
### 已完成内容:
- 已完成任务 #3: 为现有 `Task`、`TaskStage`、`taskRecorder` 和恢复流程建立基线测试，先锁定用户可感知的持久化行为。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 建立对话框、卡片与设置页基线测试
### 已完成内容:
- 已完成任务 #4: 为现有默认卡片、文件选择对话框和设置页注入路径建立基线测试，避免 UI 迁移无护栏。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 创建 app.feature_pack.api 目录骨架
### 已完成内容:
- 已完成任务 #5: 先建立新版宿主作者 API 的目录与导出骨架，为后续所有类型定义提供稳定落点。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 Manifest 数据模型
### 已完成内容:
- 已完成任务 #6: 在新版 API 中正式定义 `Manifest` 数据模型，固定 Pack 元数据的 Python 侧表达。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 实现 manifest.toml 读取与校验器
### 已完成内容:
- 已完成任务 #7: 把 `manifest.toml` 的读取、解析和错误报告收敛成正式读取器，而不是散在 `FeatureService` 里做弱校验。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 补全所有 Feature Pack 的 manifest.toml
### 已完成内容:
- 已完成任务 #8: 把现有每个 Pack 的 `manifest.toml` 升级到新版字段集合，消除仅有 `entry/dependencies` 的弱结构。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义新版 FeaturePack 抽象接口
### 已完成内容:
- 已完成任务 #9: 在新版 API 中正式定义 `FeaturePack` 抽象接口，直接以 `accepts/createTask/owns` 为中心，不再继续围绕旧接口扩展。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 TaskConfig
### 已完成内容:
- 已完成任务 #10: 在新版 API 中正式定义 `TaskConfig`，把用户可编辑的任务配置集中到一个稳定模型。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 TaskInput
### 已完成内容:
- 已完成任务 #11: 在新版 API 中正式定义 `TaskInput`，让宿主进入 Pack 前先整理成统一输入模型。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 StageSnapshot 与 TaskSnapshot
### 已完成内容:
- 已完成任务 #12: 在新版 API 中定义 Qt-free 的快照模型，为 UI、浏览器和测试提供稳定投影。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义新版 TaskStage 基类
### 已完成内容:
- 已完成任务 #13: 在新版 API 中正式定义 `TaskStage` 基类，使其成为 Pack 主要能力承载点。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义新版 Task 基类
### 已完成内容:
- 已完成任务 #14: 在新版 API 中正式定义 `Task` 作为工作流容器，固定任务级配置、编排与快照边界。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 SingleFileTask
### 已完成内容:
- 已完成任务 #15: 抽出 `SingleFileTask`，把单文件任务的路径、命名和输出同步共性正式上移。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 TaskFile
### 已完成内容:
- 已完成任务 #16: 为多文件任务建立正式的 `TaskFile` 数据模型，承载路径、大小、选择状态和摘要信息。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 MultiFileTask
### 已完成内容:
- 已完成任务 #17: 抽出 `MultiFileTask`，让多文件任务和分集选择共用同一套任务容器与选择接口。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 TaskForm 与 FormField
### 已完成内容:
- 已完成任务 #18: 建立声明式任务编辑表单模型，为默认配置对话框提供宿主统一输入。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 实现默认 TaskConfigDialog
### 已完成内容:
- 已完成任务 #19: 建立宿主默认 `TaskConfigDialog`，让任务配置编辑不再依赖每个 Pack 自己写对话框。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 实现默认 MultiFileSelectDialog
### 已完成内容:
- 已完成任务 #20: 把现有文件树选择能力提升为正式宿主对话框，供多文件任务和分集任务统一复用。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 定义 SettingSection 与 SettingItem
### 已完成内容:
- 已完成任务 #21: 建立声明式设置模型，为 Pack 设置页注入提供统一边界。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 实现 SettingsInstaller
### 已完成内容:
- 已完成任务 #22: 在宿主侧实现设置注入器，让 Pack 设置统一进入 SettingPage，而不是各 Pack 自己操作页面布局。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 改造默认 TaskCard 与 ResultCard 的编辑入口
### 已完成内容:
- 已完成任务 #23: 让默认卡片不再自己持有复杂编辑逻辑，而是统一通过宿主 `editTask()` 入口修改任务配置。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 FeatureService 的 manifest 发现与依赖排序
### 已完成内容:
- 已完成任务 #24: 在新版模块中实现 `FeatureService` 的发现和依赖排序能力，为最终切换做准备。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 FeatureService 的 Pack 加载与实例管理
### 已完成内容:
- 已完成任务 #25: 在新版模块中实现 `FeatureService` 的 Pack 加载与实例管理，为最终切换准备新版加载器。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 FeatureService 的 source 与 task 路由
### 已完成内容:
- 已完成任务 #26: 在新版模块中实现按 source 和 task 进行 Pack 路由的逻辑，为最终宿主切换提供新版路由能力。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 FeatureService 的卡片、设置与编辑分发
### 已完成内容:
- 已完成任务 #27: 在新版模块中实现卡片创建、设置注入和任务编辑分发，让宿主后续能完整使用新版服务。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 taskRecorder 持久化
### 已完成内容:
- 已完成任务 #28: 在新版模型下重建任务持久化与恢复逻辑，使任务记录不再依赖旧 V0 模型。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 BrowserService 快照与任务操作映射
### 已完成内容:
- 已完成任务 #29: 在新版模型下重建浏览器扩展的快照与任务操作映射，使浏览器入口面向稳定的任务投影。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 CoreService 的 createTask 调度入口
### 已完成内容:
- 已完成任务 #30: 让 `CoreService` 的新版实现面向 `TaskInput`、新版 `FeatureService` 和新版 `Task` 调度。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 在新版模块中实现 AddTaskDialogSession 的 TaskInput 流转
### 已完成内容:
- 已完成任务 #31: 把添加任务对话框和解析会话重写到 `TaskInput` / `TaskConfig` 流程上，为最终宿主切换做准备。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 实现 TaskCard -> Task -> TaskStage 命令链
### 已完成内容:
- 已完成任务 #32: 把用户命令链明确落到 `TaskCard -> Task -> TaskStage`，避免 UI 继续直接修改工作流内部状态。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-13 - 任务: 实现 TaskStage -> Task -> TaskCard 事件投影链
### 已完成内容:
- 已完成任务 #33: 把阶段事件先汇总到任务，再投影给卡片和 UI，符合 Actor 风格的状态所有权边界。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-14 - 任务: 改写 http_pack 到新版接口
### 已完成内容:
- 已完成任务 #34: 把 `http_pack` 改写为新版 `FeaturePack`、`SingleFileTask`、`TaskConfig` 和默认编辑流程的首个生产落点。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-14 - 任务: 改写 extract_pack 到新版接口
### 已完成内容:
- 已完成任务 #35: 把 `extract_pack` 改写为新版能力型 Pack，为其他 Pack 提供正式的解压工作流组件。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-14 - 任务: 改写 ffmpeg_pack 的安装任务
### 已完成内容:
- 已完成任务 #36: 先单独改写 `ffmpeg_pack` 的安装任务，使运行时依赖安装路径先落到新版接口。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-14 - 任务: 改写 ffmpeg_pack 的合并任务
### 已完成内容:
- 已完成任务 #37: 再单独改写 `ffmpeg_pack` 的合并任务，使媒体合并工作流落到新版 `SingleFileTask` 和 Stage 模型上。
### 测试验证:
- 通过: 当轮已执行 `uv run basedpyright ...`、`uv run python tests/test_automation_contracts.py` 与相关 `uv run python tests/...` 验证；精确命令明细已压缩，必要时查对应提交。
### 备注:
- 对齐摘要: 范围、约束、验收和依赖均来自两份 Feature Pack V1 迁移文档与 `task.json`；本压缩版仅保留继续推进所需的审计信息。
---
## 2026-04-28 - 任务: 改写 github_pack 到新版接口
### 已完成内容:
- 已将 `github_pack` 入口改为新版 `FeaturePack.accepts/createTask/owns` 接口，并保留旧 `canHandle/parse/createTaskFromPayload` 兼容入口。
- 新增 `GitHubDownloadTask` 与 `GitHubHttpTaskStage`，使用 `github_pack/github_download@1` 稳定身份，同时复用新版 `http_pack` 的探测、单文件下载 Stage 和默认卡片能力。
- GitHub 链接创建任务时保留原始 `TaskConfig.source`，将实际下载 Stage URL 改写为所选 GitHub 代理站地址；编辑任务后继续通过 `configure(TaskConfig)` 同步代理 URL 与输出路径。
- 为新版服务路由补充显式 `priority` 兼容逻辑，使启用后的 `github_pack` 可在泛 HTTP Pack 前处理 GitHub 文件链接，未声明 priority 的 Pack 仍按原加载顺序路由。
- 新增 `tests/feature_pack/test_github_pack_v1.py`，覆盖 GitHub URL 识别、代理改写、服务路由、任务归属、默认卡片和编辑后源地址保持。
### 测试验证:
- 初次运行 `uv run python tests/feature_pack/test_github_pack_v1.py` 失败，原因为本地 `.venv` 中 `pyside6-fluent-widgets` 元数据存在但 `qfluentwidgets` 包目录缺失；执行 `uv pip install --force-reinstall pyside6-fluent-widgets==1.11.2` 后再执行 `uv sync --all-groups` 修复环境并恢复锁定的 PySide6 版本。
- 通过: `uv run python tests/feature_pack/test_github_pack_v1.py`。
- 初次运行 `uv run basedpyright tests/test_automation_contracts.py app/feature_pack/api/service.py features/github_pack/pack.py features/github_pack/task.py tests/feature_pack/test_github_pack_v1.py` 发现新文件类型告警；修正后再次运行通过，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests/test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #38，目标范围仅限把 `github_pack` 改写为基于新版接口和 `http_pack` 单文件下载能力的轻量组合 Pack；关键约束是每轮只处理这一项任务、保持 `task.json` 为唯一任务状态、验证通过后才能写 `passes=true`，且 `progress.txt` 必须先记录中文证据；验收标准是 Pack 与任务入口使用 V1 `FeaturePack/TaskInput/TaskConfig/Task` 语义，GitHub 解析和卡片路径有测试覆盖，并通过静态检查与测试脚本；依赖顺序上本任务位于已完成的新版 API、默认 UI、服务路由、`http_pack` 和 `ffmpeg_pack` 改写之后，直接依赖 `http_pack`；主要风险是 GitHub URL 与泛 HTTP URL 路由重叠、代理 URL 不应污染用户可编辑的原始 source、以及复用 HTTP Stage 时必须使用 `github_pack` 自己的持久化身份。
---
## 2026-04-28 - 任务: 改写 ftp_pack 到新版接口
### 已完成内容:
- 已将 `ftp_pack` 入口改为新版 `FeaturePack.accepts/createTask/owns`，并保留 `canHandle/parse/createTaskFromPayload` 兼容入口。
- 已将 `FtpTask` 改为基于 `MultiFileTask` 与 `TaskConfig` 的多文件任务，文件条目使用稳定 `TaskFile.id`，选择、统计、快照和持久化都进入新版模型。
- 已将 `FtpTaskStage` 改为新版 `TaskStage`，用稳定阶段身份记录 FTP 下载状态，并通过 `configure(TaskConfig)` 接收代理与分块数。
- 已让 FTP 任务编辑使用默认卡片、默认 `TaskConfigDialog` 和默认 `MultiFileSelectDialog` 流程，路径同步由 `syncOutput()` 统一下发到阶段输出路径。
- 新增 `tests/feature_pack/test_ftp_pack_v1.py` 覆盖 FTP 多文件创建、默认编辑/选择、路径同步和 V1 recorder 恢复；同时把旧持久化基线中的 FTP 样例改成测试内本地旧模型，避免生产 `ftp_pack` 继续依赖 V0。
### 测试验证:
- 通过: `uv run python tests\feature_pack\test_ftp_pack_v1.py`。
- 通过: `uv run python tests\feature_pack\test_task_persistence_baseline.py`。
- 通过: `uv run basedpyright tests/test_automation_contracts.py features/ftp_pack/task.py features/ftp_pack/pack.py tests/feature_pack/test_ftp_pack_v1.py tests/feature_pack/test_task_persistence_baseline.py`，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests\test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #39，目标范围仅限把 `ftp_pack` 改写为新版 `MultiFileTask` 代表实现；关键约束是每轮只处理这一项任务、验证通过后先写入中文 `progress.txt` 证据再更新 `task.json`、不把旧 Feature Pack 接口作为目标、文件选择/路径同步/配置修改必须通过 `MultiFileTask`、`TaskConfig`、`configure()` 和默认对话框流程完成；验收标准是 `FtpTask`、相关 Stage 和 Pack 入口进入 V1 `FeaturePack/TaskInput/TaskConfig/TaskStage` 语义，FTP 多文件创建、选择、路径同步、默认卡片编辑和持久化恢复都有测试覆盖，并通过静态检查与全部测试脚本；依赖顺序上本任务位于新版 API、默认 UI、服务路由、持久化、Core/AddTask 流程以及 `http_pack/github_pack` 改写之后，后续 `bittorrent_pack` 和 `bili_pack` 的多文件迁移会复用该路径；主要风险是 FTP 远端 source 变更需要重新列目录，当前默认编辑表单因此不暴露 source 字段，避免在同步配置时猜测远端文件列表。
---
## 2026-04-28 - 任务: 改写 bittorrent_pack 到新版接口
### 已完成内容:
- 已将 `bittorrent_pack` 入口改为新版 `FeaturePack.accepts/createTask/owns`，并保留 `canHandle/parse/createTaskFromPayload` 兼容入口。
- 已将 `BitTorrentTask` 改为基于 `MultiFileTask` 与 `TaskConfig` 的多文件任务，种子文件条目使用稳定 `TaskFile.id`，文件选择、优先级、输出根目录和快照统计进入新版模型。
- 已将 `BitTorrentTaskStage` 改为新版 `TaskStage`，使用 `bittorrent_pack/bittorrent_download@1` 稳定身份，运行状态、传输进度、错误和 resume 相关状态进入 V1 持久化。
- 已让 BitTorrent 任务编辑使用默认卡片、默认 `TaskConfigDialog` 和默认 `MultiFileSelectDialog` 流程；本地 `.torrent`、HTTP `.torrent` 和 magnet 来源继续由 Pack 入口路由。
- 新增 `tests/feature_pack/test_bittorrent_pack_v1.py`，覆盖本地种子多文件创建、默认编辑选择、V1 recorder 恢复和浏览器快照摘要。
### 测试验证:
- 通过: `uv run python tests\feature_pack\test_bittorrent_pack_v1.py`。
- 通过: `uv run basedpyright tests/test_automation_contracts.py features/bittorrent_pack/task.py features/bittorrent_pack/pack.py tests/feature_pack/test_bittorrent_pack_v1.py`，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests\test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #40，目标范围仅限把 `bittorrent_pack` 改写为新版多文件任务实现，覆盖 `BitTorrentTask`、相关 Stage、Pack 入口、种子文件选择、恢复和浏览器快照投影；关键约束是每轮只处理这一项任务、完成全部列出的 steps，验证通过前不得把 `task.json` 中本任务标记为通过，且 `progress.txt` 必须记录中文证据；验收标准是 BitTorrent 入口使用 V1 `FeaturePack/TaskInput/TaskConfig/MultiFileTask/TaskStage` 语义，文件选择进入 `MultiFileTask.select()` 和默认多文件选择流程，任务恢复与 `TaskSnapshot` 投影可被测试覆盖，并通过静态检查与 `tests/` 下 Python 测试脚本；依赖顺序上本任务位于已完成的新版 API、默认 UI、FeatureService、持久化、浏览器快照、Core/AddTask、`http_pack/github_pack/ftp_pack` 迁移之后，直接复用前一轮 FTP 多文件路径；主要风险是 BitTorrent 种子元信息、选中文件索引和下载引擎配置之间存在映射关系，恢复时必须保持稳定文件 id，不应再依赖 V0 类名或匿名 payload 猜测状态。
- 运行全量测试时 Qt 输出了缺失字体目录、offscreen 插件和 Windows theme 相关警告，但测试进程退出码为 0，未影响本轮验证结论。
---
## 2026-04-28 - 任务: 改写 bili_pack 的工作流与分集选择
### 已完成内容:
- 已将 `bili_pack` 入口改为新版 `FeaturePack.accepts/createTask/owns`，并保留 `canHandle/parse/createTaskFromPayload` 兼容入口。
- 已将 `BilibiliTask` 改为基于 `MultiFileTask` 与 `TaskConfig` 的分集任务，分集条目使用稳定 `TaskFile.id`，选择、统计、快照、路径同步和持久化进入新版模型。
- 已新增 `BilibiliDownloadStage` 与 `BilibiliMergeStage`，使用 `bili_pack/bilibili_download@1` 稳定任务身份，并为 HTTP 下载与 FFmpeg 合并阶段注册 Bili 自有持久化身份。
- 已让分集选择进入默认 `TaskConfigDialog` 与 `MultiFileSelectDialog` 流程，默认卡片可通过 `files` 字段选择分集，并通过 `configure(TaskConfig)` 同步输出目录、名称、代理和分块数。
- 新增 `tests/feature_pack/test_bili_pack_v1.py`，覆盖 Bilibili 多分集创建、默认编辑分集选择、阶段路径同步、V1 recorder 恢复和浏览器快照摘要。
### 测试验证:
- 初次运行 `uv run python tests\feature_pack\test_bili_pack_v1.py` 失败，原因是测试临时 Pack 工作区缺少 `ffmpeg_pack` 的 `extract_pack` 依赖；补齐夹具后继续验证。
- 第二次运行 `uv run python tests\feature_pack\test_bili_pack_v1.py` 失败，原因是测试 patch 目标指向了未被服务使用的模块名，且 Bili 阶段子类需要显式覆盖继承的抽象方法；修正后再次运行通过。
- 通过: `uv run python tests\feature_pack\test_bili_pack_v1.py`。
- 通过: `uv run basedpyright tests/test_automation_contracts.py features/bili_pack/task.py features/bili_pack/pack.py tests/feature_pack/test_bili_pack_v1.py`，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests\test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #41，目标范围仅限改写 `bili_pack` 的核心工作流、`BilibiliTask`、主要下载/合并 Stage 和分集选择；关键约束是每轮只处理这一项任务、完成全部列出的 steps，验证通过前不得更新 `task.json`，且必须先在 `progress.txt` 写入中文证据；验收标准是 Bili 任务使用 V1 `FeaturePack/TaskInput/TaskConfig/MultiFileTask/TaskStage` 语义，分集选择通过 `MultiFileTask.select()` 与默认选择对话框流程完成，分集选择和工作流有测试覆盖，并通过静态检查和全部测试脚本；依赖顺序上本任务位于已完成的新版 API、默认 UI、FeatureService、持久化、浏览器快照、Core/AddTask、`http_pack`、`ffmpeg_pack`、`ftp_pack` 与 `bittorrent_pack` 迁移之后，直接复用 HTTP 下载、FFmpeg 合并和多文件选择路径；主要风险是 Bilibili 分集、音视频下载阶段与合并阶段之间必须保持稳定映射，恢复时不能依赖 V0 类名或匿名 payload，并且分集选择不应绕过默认多文件选择流程。
- 运行全量测试时 Qt 输出了缺失字体目录、offscreen 插件、Windows theme 以及既有基线日志相关警告，但测试进程退出码为 0，未影响本轮验证结论。
---
## 2026-04-28 - 任务: 改写 bili_pack 的设置与卡片
### 已完成内容:
- 已让 `bili_pack` 通过 `BilibiliPack.settingSection()` 返回 `SettingSection`，设置页入口进入新版宿主 `DefaultSettingsInstaller.install()` 路径。
- 已为新版设置注入器补充 `custom` 设置项支持，使 Bilibili 的清晰度、HDR、杜比视界和账号登录卡片继续使用 Pack 自有配置项与自定义登录卡。
- 已将 Bilibili 设置卡创建收敛为可复用工厂，旧 `loadSettingCards()` 仍复用同一批卡片创建逻辑，避免设置 UI 行为分叉。
- 已验证 Bilibili 的 TaskCard、ResultCard 均回落到新版默认卡片，并且运行中编辑与创建前编辑都通过宿主 `editTask()` / `TaskConfigDialog` 流程提交分集选择和配置变更。
- 已扩展 `tests/feature_pack/test_bili_pack_v1.py`，覆盖 `SettingSection` 安装、Bilibili 自定义设置卡、默认 TaskCard、默认 ResultCard 和编辑入口模式。
### 测试验证:
- 通过: `uv run python tests\feature_pack\test_bili_pack_v1.py`。
- 初次运行 `uv run basedpyright tests/test_automation_contracts.py app/feature_pack/api/service.py features/bili_pack/config.py features/bili_pack/pack.py tests/feature_pack/test_bili_pack_v1.py` 失败，原因是本轮首次把遗留 `features/bili_pack/config.py` 纳入静态检查后暴露既有 qfluentwidgets/旧配置 UI 类型噪声；补充与迁移文件一致的 pyright 文件级抑制后再次运行通过。
- 通过: `uv run basedpyright tests/test_automation_contracts.py app/feature_pack/api/service.py features/bili_pack/config.py features/bili_pack/pack.py tests/feature_pack/test_bili_pack_v1.py`，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests\test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #42，目标范围仅限改写 `bili_pack` 的设置注入、TaskCard、ResultCard 和编辑入口；关键约束是每轮只处理这一项任务、完成全部列出的 steps，验证通过前不得更新 `task.json`，且必须先在 `progress.txt` 写入中文证据；验收标准是 Bili 设置页通过 `SettingSection` 进入新版宿主 UI，卡片层通过默认 `DefaultTaskCard/DefaultResultCard` 和宿主 `editTask()` 流程完成配置与分集选择，并有设置与卡片测试覆盖；依赖顺序上本任务位于已完成的新版 API、默认 UI、FeatureService、默认设置注入器、默认卡片、`bili_pack` 工作流与分集选择之后，直接复用上一轮的 `BilibiliTask.editForm()` 与 `MultiFileTask.select()` 路径；主要风险是 Bilibili 设置存在自定义登录卡和真实配置项，不能用宿主临时 ConfigItem 替代，否则解析清晰度、HDR、杜比和 Cookie 会与实际 Pack 配置脱节。
- 运行全量测试时 Qt 输出了缺失字体目录、offscreen 插件、Windows theme、既有基线日志以及 QFluentWidgets Pro 提示，但测试进程退出码为 0，未影响本轮验证结论。
---
## 2026-04-28 - 任务: 改写 m3u8_pack 的下载工作流
### 已完成内容:
- 已将 `M3U8Pack` 入口改为新版 `FeaturePack.accepts/createTask/owns`，并保留 `canHandle/parse/createTaskFromPayload` 兼容入口。
- 已将 `M3U8Task` 改为基于新版 `SingleFileTask` 与 `TaskConfig` 的单文件流媒体下载任务，输出文件名、目录、请求头、代理和线程数统一通过 `configure(TaskConfig)` 更新。
- 已将 `M3U8TaskStage` 改为新版 `TaskStage`，使用 `m3u8_pack/m3u8_download@1` 稳定身份，提供快照、状态投影、进度更新和 V1 持久化恢复。
- 已将下载输出同步收敛到 `syncOutput()`，由任务统一把最终输出路径和 `.gd3_m3u8` 临时目录同步给主下载 Stage。
- 新增 `tests/feature_pack/test_m3u8_pack_v1.py`，覆盖 M3U8 优先路由、任务创建、配置同步、下载阶段运行和 V1 recorder 恢复。
### 测试验证:
- 通过: `uv run python tests\feature_pack\test_m3u8_pack_v1.py`。
- 初次运行 `uv run basedpyright tests/test_automation_contracts.py features/m3u8_pack/task.py features/m3u8_pack/pack.py tests/feature_pack/test_m3u8_pack_v1.py` 发现迁移文件的既有类型噪声与少量新增注解告警；补齐注解与文件级 pyright 抑制后再次运行通过。
- 通过: `uv run basedpyright tests/test_automation_contracts.py features/m3u8_pack/task.py features/m3u8_pack/pack.py tests/feature_pack/test_m3u8_pack_v1.py`，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests\test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #43，目标范围仅限改写 `m3u8_pack` 的下载工作流、`M3U8Task` 和主下载 Stage；关键约束是每轮只处理这一项任务、完成全部列出的 steps，验证通过前不得更新 `task.json`，且必须先在 `progress.txt` 写入中文证据；验收标准是流媒体下载核心路径使用 V1 `FeaturePack/TaskInput/TaskConfig/SingleFileTask/TaskStage` 语义，输出同步走 `syncOutput()`，并有下载工作流测试覆盖；依赖顺序上本任务位于已完成的新版 API、默认 UI、FeatureService、持久化、Core/AddTask、`http_pack`、`ffmpeg_pack` 和 `bili_pack` 迁移之后，仍把安装任务、设置注入和附属 UI 留给后续任务 #44；主要风险是 M3U8 与泛 HTTP 路由重叠、N_m3u8DL-RE 输出文件可能修正实际扩展名、以及下载进度只能从外部进程文本解析，测试因此使用假的 manifest 响应和下载 Worker 覆盖核心工作流而不触发真实网络和外部进程。
- 运行全量测试时 Qt 输出了缺失字体目录、offscreen 插件、Windows theme、既有基线日志以及 QFluentWidgets Pro 提示，但测试进程退出码为 0，未影响本轮验证结论。
---
## 2026-04-28 - 任务: 改写 m3u8_pack 的安装与设置
### 已完成内容:
- 已将 `M3U8InstallTask` 从 V0 `LegacyTask` 改为新版 `Task`，使用 `m3u8_pack/m3u8_install@1` 稳定任务身份、`TaskConfig` 和 `TaskSnapshot`。
- 已新增 `M3U8InstallDownloadStage` 与 `M3U8InstallExtractStage`，安装下载与解压阶段使用 `http_download@1`、`extract_archive@1` 阶段身份并注册到 `m3u8_install` 任务下。
- 已让 `createInstallTask()` 直接组装新版安装任务和阶段，不再通过旧 `coreService._parseUrl()` 借出旧安装流程。
- 已为 `M3U8Pack` 增加 `gd3+m3u8://install` 新版安装入口，并让 `accepts/createTask/owns/parse/createTaskFromPayload` 都能识别下载任务和安装任务。
- 已把 `m3u8_pack` 设置页改为 `SettingSection` + `custom` 设置项声明，旧 `loadSettingCards()` 复用同一批设置卡工厂，避免新旧设置 UI 行为分叉。
- 已验证 M3U8 下载任务和安装任务都回落到新版默认卡片，并通过宿主 `editTask()` / `TaskConfigDialog` 入口修改配置。
- 已扩展 `tests/feature_pack/test_m3u8_pack_v1.py`，覆盖安装任务创建、安装运行与恢复、设置注入、默认 TaskCard/ResultCard 编辑入口和 M3U8 manifest scheme。
### 测试验证:
- 初次运行 `uv run python tests\feature_pack\test_m3u8_pack_v1.py` 失败，原因是安装解压后的可执行文件路径断言仍按旧嵌套目录预期；修正为实际安装根目录后再次运行通过。
- 初次运行 `uv run python -m unittest -v tests.feature_pack.test_m3u8_pack_v1.M3U8PackV1Tests.testM3U8PackInstallsSettingsThroughSettingSection` 在 offscreen 环境创建真实运行时设置卡时触发 Qt access violation；测试改为替换 runtime card factory，只验证宿主设置注入边界后通过。
- 通过: `uv run python tests\feature_pack\test_m3u8_pack_v1.py`。
- 初次运行 `uv run basedpyright tests/test_automation_contracts.py features/m3u8_pack/task.py features/m3u8_pack/pack.py features/m3u8_pack/config.py tests/feature_pack/test_m3u8_pack_v1.py tests/feature_pack/test_repository_manifests.py` 失败，原因是本轮首次把 `features/m3u8_pack/config.py` 纳入静态检查后暴露既有 qfluentwidgets/旧配置 UI 类型噪声，以及新增安装阶段快照返回注解缺失；补充文件级抑制和返回类型后再次运行通过。
- 通过: `uv run basedpyright tests/test_automation_contracts.py features/m3u8_pack/task.py features/m3u8_pack/pack.py features/m3u8_pack/config.py tests/feature_pack/test_m3u8_pack_v1.py tests/feature_pack/test_repository_manifests.py`，结果为 `0 errors, 0 warnings, 0 notes`。
- 通过: `uv run python tests\test_automation_contracts.py`。
- 通过: `$tests = Get-ChildItem tests -Recurse -Filter 'test_*.py' | Sort-Object FullName; foreach ($test in $tests) { uv run python $test.FullName; if ($LASTEXITCODE -ne 0) { Write-Error "Failed: $($test.FullName)"; exit $LASTEXITCODE } }`，已执行 `tests/` 下全部 `test_*.py` 脚本。
### 备注:
- 对齐摘要: 当前任务是 `task.json` 中第一个 `passes = false` 的任务 #44，目标范围仅限改写 `m3u8_pack` 的安装任务、设置注入和附属 UI；关键约束是每轮只处理这一项任务、完成全部列出的 steps，验证通过前不得更新 `task.json`，且必须先在 `progress.txt` 写入中文证据；验收标准是 `M3U8InstallTask` 和相关安装 Stage 使用 V1 `Task/TaskStage/TaskConfig/TaskSnapshot` 语义，设置页通过 `SettingSection` 进入新版宿主 UI，编辑入口通过默认卡片和宿主 `editTask()` 完成，并有安装与设置测试覆盖；依赖顺序上本任务位于已完成的新版 API、默认 UI、FeatureService、持久化、`http_pack`、`extract_pack`、`ffmpeg_pack` 和上一轮 `m3u8_pack` 下载工作流之后；主要风险是安装阶段必须复用 HTTP 下载与解压能力但不能沿用 V0 任务身份，设置页存在真实运行时检测和一键安装按钮，测试需要避开 offscreen 环境下真实 runtime card 的 Qt 崩溃点而仍验证宿主注入契约。
- 运行全量测试时 Qt 输出了缺失字体目录、offscreen 插件、Windows theme、既有基线日志以及 QFluentWidgets Pro 提示，但测试进程退出码为 0，未影响本轮验证结论。
---
